---
title: "Senators"
format:
  html:
    page-layout: full
execute:
  echo: false
  warning: false
  message: false
---

```{r}
library(reactable)
source("scripts/shared.R")

# Pull any newly uploaded role profiles before deciding whether the
# Profile column appears
sync_intake_profiles()

senators <- load_senators() |>
  mutate(display_name = paste(Last.Name, First.Name, sep = ", ")) |>
  arrange(District)

# Profile PDFs exist once role profiles are posted; the column only
# appears when the folder has files. Filenames follow name_link:
# e.g. ochoa_bogh_19_profile.pdf
profiles_exist <- dir.exists(SEN_PROFILE_DIR) && length(list.files(SEN_PROFILE_DIR)) > 0

leadership_has_data <- "Leadership" %in% names(senators) &&
  any(!is.na(senators$Leadership) & senators$Leadership != "")

tbl <- senators |> select(District, display_name, Party)
if (leadership_has_data) tbl$Leadership <- senators$Leadership
if (profiles_exist) {
  tbl$Profile <- sprintf("%s/%s_%s_profile.pdf",
                         SEN_PROFILE_DIR, senators$name_link, senators$District)
}

cols <- list(
  District = colDef(width = 90),   # numeric: sorts 1, 2, … 40
  display_name = colDef(
    name = "Name",
    cell = link_cell("", sprintf("%s/district_%s.html", SENATOR_PAGES_DIR, senators$District))
  ),
  Party = colDef(width = 70)
)
if (profiles_exist) {
  cols$Profile <- colDef(
    sortable = FALSE, width = 110,
    cell = function(value) htmltools::tags$a(href = value, "View Profile")
  )
}

legsim_table(tbl, page_size = 20, columns = cols)
```
